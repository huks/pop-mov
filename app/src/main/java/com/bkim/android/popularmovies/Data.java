package com.bkim.android.popularmovies;

public class Data {
    static final String BASE = "http://image.tmdb.org/t/p/";
    static final String SIZE = "w815";
    static final String EXT = ".jpg";

    static final String foobar = "nBNZadXqJSdt05SHLqgT0HuC5Gm";

    static final String[] URLS = {
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT, BASE + foobar + EXT, BASE + foobar + EXT,
            BASE + foobar + EXT
    };

    private Data() {
        // No instances.
    }
}
